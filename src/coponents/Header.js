import React from "react";
import styled from "styled-components";

const StTitle = styled.h1`
  text-align: center;
  font-size: 5rem;
`;

const Header = () => {
  return <StTitle>Weather</StTitle>;
};
export default Header;
